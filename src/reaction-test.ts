export interface ReactionTestUser {
  id: number;
  nickname: string | null;
  profile_image_url: string | null;
}

const escapeHtml = (value: string): string => value.replace(/[&<>"']/g, (character) => ({
  "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;",
}[character] ?? character));

const profileMarkup = (user: ReactionTestUser): string => {
  const image = user.profile_image_url?.startsWith("https:")
    ? `<img class="mini-avatar" src="${escapeHtml(user.profile_image_url)}" alt="프로필 이미지">`
    : `<span class="mini-avatar avatar-placeholder">K</span>`;
  return `${image}<a class="user-link" href="/account">${escapeHtml(user.nickname || "내 정보")}</a>`;
};

export const renderReactionTestPage = (user: ReactionTestUser | null): string => `<!doctype html>
<html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Reaction Test</title>
<style>
:root{font-family:system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif;color-scheme:light}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:#f5f6f8;color:#191919}a{color:inherit}.topbar{min-height:64px;display:flex;align-items:center;justify-content:space-between;padding:0 max(16px,calc((100vw - 900px)/2));background:#fff;border-bottom:1px solid #e7e7e7}.brand{font-weight:900;text-decoration:none;letter-spacing:-.03em}.user-area{display:flex;align-items:center;gap:10px;font-size:14px}.mini-avatar{width:32px;height:32px;border-radius:50%;object-fit:cover;background:#eee;display:grid;place-items:center}.avatar-placeholder{font-weight:800}.user-link{font-weight:700;text-decoration:none}.login-link{padding:9px 13px;border-radius:9px;background:#fee500;text-decoration:none;font-weight:800}main{width:min(94vw,900px);margin:auto;padding:30px 0 56px}.back{font-size:14px;color:#666;text-decoration:none}.hero{margin:20px 0}.eyebrow{margin:0 0 8px;color:#777;font-size:13px;font-weight:800;letter-spacing:.08em}h1{margin:0 0 8px;font-size:clamp(30px,5vw,44px);letter-spacing:-.04em}.subtitle{margin:0;color:#666}.panel{background:#fff;border:1px solid #e5e5e5;border-radius:20px;box-shadow:0 10px 30px rgba(0,0,0,.06);padding:22px}.intro{text-align:center;padding:28px}.intro .signal{font-size:64px;font-weight:950;letter-spacing:-.06em}.primary{width:100%;padding:14px;border:0;border-radius:12px;background:#191919;color:#fff;font:inherit;font-weight:900;cursor:pointer}.rule{margin:16px 0 0;color:#777;font-size:13px;line-height:1.6}.login-note{margin-top:14px;padding:12px;border-radius:10px;background:#fff8e1;color:#7a5a00;font-size:13px}.hud{display:grid;grid-template-columns:repeat(3,1fr);gap:10px;margin-bottom:12px}.stat{padding:12px;border:1px solid #eee;border-radius:12px;background:#fafafa;text-align:center}.stat-label{display:block;color:#888;font-size:12px}.stat-value{display:block;margin-top:2px;font-size:22px;font-weight:900}.arena{position:relative;min-height:420px;border-radius:16px;background:#111;border:1px solid #222;display:grid;place-items:center;cursor:pointer;user-select:none;overflow:hidden}.arena.wait{background:#171717}.arena.go{background:#fff;color:#111;cursor:pointer}.arena.false{background:#f3e1e1}.arena.timeout{background:#eee}.signal-text{text-align:center;font-size:clamp(42px,9vw,88px);font-weight:950;letter-spacing:-.06em}.signal-sub{display:block;margin-top:8px;text-align:center;font-size:14px;color:#999}.go .signal-sub{color:#666}.round-list{margin-top:14px;display:grid;gap:7px}.round{display:flex;justify-content:space-between;padding:9px 12px;border-radius:9px;background:#f7f7f7;font-size:13px}.round strong{font-size:14px}.result{text-align:center;display:none}.result h2{margin:0 0 8px}.grade{font-size:38px;font-weight:950}.average{font-size:54px;font-weight:950}.average small{font-size:18px;color:#777}.actions{display:flex;justify-content:center;gap:10px;margin-top:18px;flex-wrap:wrap}.actions a,.actions button{padding:11px 15px;border-radius:10px;border:1px solid #ddd;background:#fff;text-decoration:none;font:inherit;font-weight:800;cursor:pointer}.actions .primary{width:auto;background:#191919;color:#fff;border-color:#191919}.hidden{display:none!important}@media(max-width:640px){.user-link{display:none}main{padding-top:24px}.panel{padding:16px}.arena{min-height:320px}.hud{grid-template-columns:1fr 1fr}.hud .stat:last-child{grid-column:1/-1}}
</style></head>
<body><header class="topbar"><a class="brand" href="/">MINI GAME PORTAL</a><div class="user-area">${user ? `${profileMarkup(user)}<button id="logout" class="user-link" style="border:0;background:transparent;cursor:pointer;font:inherit">로그아웃</button>` : `<a class="login-link" href="/auth/kakao">카카오로 로그인</a>`}</div></header>
<main><a class="back" href="/">← 게임 목록</a><section class="hero"><p class="eyebrow">MINI GAME · 02</p><h1>Reaction Test</h1><p class="subtitle">신호가 나타나는 순간 최대한 빠르게 클릭하세요.</p></section>
<section id="setup" class="panel intro"><p class="eyebrow">5 ROUNDS</p><div class="signal">⚡</div><h2>반응 속도 테스트</h2><p class="rule">준비 후 화면이 <b>GO!</b>로 바뀌면 즉시 클릭하세요.<br>너무 빨리 누르면 FALSE START, 1.5초 안에 누르지 않으면 TIMEOUT입니다.<br>3라운드 이상 성공해야 랭킹에 등록됩니다.</p><button id="start" class="primary">테스트 시작</button>${user ? "" : `<div class="login-note">게스트로 테스트할 수 있습니다. 로그인하면 결과를 랭킹에 등록할 수 있습니다.</div>`}</section>
<section id="game" class="panel hidden"><div class="hud"><div class="stat"><span class="stat-label">ROUND</span><span id="round" class="stat-value">1 / 5</span></div><div class="stat"><span class="stat-label">성공</span><span id="success" class="stat-value">0</span></div><div class="stat"><span class="stat-label">최고 반응</span><span id="best" class="stat-value">-</span></div></div><div id="arena" class="arena"><div><div id="signal" class="signal-text">READY</div><span id="signalSub" class="signal-sub">신호를 기다리세요</span></div></div><div id="roundList" class="round-list"></div></section>
<section id="result" class="panel result"><p class="eyebrow">TEST COMPLETE</p><h2 id="resultTitle">결과</h2><div id="grade" class="grade">-</div><div id="average" class="average">-<small> ms</small></div><div id="resultDetail" class="rule"></div><div id="submitStatus" class="rule"></div><div class="actions"><button id="retry" class="primary">다시 테스트</button><a href="/games/reaction/ranking">랭킹 보기</a><a href="/">홈</a></div></section></main>
${user ? `<script>document.getElementById("logout")?.addEventListener("click",async()=>{const b=document.getElementById("logout");if(b)b.disabled=true;try{const r=await fetch("/auth/logout",{method:"POST"});if(r.ok)location.href="/";else if(b)b.disabled=false}catch{if(b)b.disabled=false}});</script>` : ""}
<script>
(()=>{const setup=document.getElementById("setup"),game=document.getElementById("game"),result=document.getElementById("result"),start=document.getElementById("start"),retry=document.getElementById("retry"),arena=document.getElementById("arena"),signal=document.getElementById("signal"),signalSub=document.getElementById("signalSub"),roundEl=document.getElementById("round"),successEl=document.getElementById("success"),bestEl=document.getElementById("best"),list=document.getElementById("roundList"),grade=document.getElementById("grade"),average=document.getElementById("average"),detail=document.getElementById("resultDetail"),status=document.getElementById("submitStatus");
let round=0,successful=0,falseStarts=0,timeouts=0,times=[],signalAt=0,waiting=false,go=false,timer=0,token=0;
const reset=()=>{window.clearTimeout(timer);token++;round=0;successful=0;falseStarts=0;timeouts=0;times=[];signalAt=0;waiting=false;go=false;list.innerHTML="";successEl.textContent="0";bestEl.textContent="-";arena.className="arena";signal.textContent="READY";signalSub.textContent="신호를 기다리세요";};
const updateHud=()=>{roundEl.textContent=round+" / 5";successEl.textContent=String(successful);bestEl.textContent=times.length?Math.min(...times)+"ms":"-";};
const addRound=(label,value)=>{const row=document.createElement("div");row.className="round";row.innerHTML="<span>ROUND "+round+" · "+label+"</span><strong>"+value+"</strong>";list.appendChild(row);};
const next=()=>{if(round>=5)return finish();round++;updateHud();arena.className="arena wait";signal.textContent="WAIT...";signalSub.textContent="신호가 나타나면 클릭";waiting=true;go=false;const myToken=token;const delay=1500+Math.floor(Math.random()*2501);timer=window.setTimeout(()=>{if(myToken!==token)return;waiting=false;go=true;arena.className="arena go";signal.textContent="GO!";signalSub.textContent="CLICK!";signalAt=performance.now();timer=window.setTimeout(()=>{if(myToken!==token||!go)return;go=false;timeouts++;arena.className="arena timeout";signal.textContent="TIMEOUT";signalSub.textContent="1.5초 안에 클릭하지 못했습니다";addRound("TIMEOUT","- ");updateHud();timer=window.setTimeout(next,900);},1500);},delay);};
const begin=()=>{reset();setup.classList.add("hidden");result.style.display="none";game.classList.remove("hidden");next();};
const finish=()=>{window.clearTimeout(timer);go=false;waiting=false;arena.className="arena";game.classList.add("hidden");result.style.display="block";const avg=times.length?Math.round(times.reduce((a,b)=>a+b,0)/times.length):0;grade.textContent=times.length>=3?(avg<200?"PERFECT":avg<250?"EXCELLENT":avg<300?"GREAT":avg<400?"GOOD":"SLOW"):"UNRANKED";average.innerHTML=times.length?avg+"<small> ms</small>":"-<small> ms</small>";detail.textContent=successful+"회 성공 · "+falseStarts+"회 조기 클릭 · "+timeouts+"회 TIMEOUT"+(times.length<3?" · 성공 3회 이상부터 랭킹 등록 가능":"");status.textContent="";if(times.length>=3)submit(avg);};
const submit=async(avg)=>{status.textContent="점수를 서버에 등록하는 중...";try{const r=await fetch("/api/games/reaction/scores",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({round_count:5,successful_rounds:successful,reaction_times:times,false_starts:falseStarts,timeouts})});if(r.status===401){status.textContent="로그인하면 이 기록을 랭킹에 등록할 수 있습니다.";return;}const data=await r.json();if(!r.ok){status.textContent=data.error||"점수 등록에 실패했습니다.";return;}status.textContent=data.is_personal_best?"🏆 개인 최고 기록을 갱신했습니다!":"기록이 랭킹에 등록되었습니다.";}catch{status.textContent="점수 등록 상태를 확인할 수 없습니다.";}};
arena.addEventListener("click",()=>{if(go){const elapsed=Math.round(performance.now()-signalAt);go=false;window.clearTimeout(timer);if(elapsed>=100&&elapsed<=1500){successful++;times.push(elapsed);addRound("SUCCESS",elapsed+"ms");arena.className="arena";signal.textContent=elapsed+"ms";signalSub.textContent="좋습니다! 다음 라운드";updateHud();timer=window.setTimeout(next,900);}return;}if(waiting){waiting=false;window.clearTimeout(timer);falseStarts++;arena.className="arena false";signal.textContent="TOO SOON!";signalSub.textContent="신호보다 먼저 클릭했습니다";addRound("FALSE START","- ");updateHud();timer=window.setTimeout(next,900);}});
start.addEventListener("click",begin);retry.addEventListener("click",()=>{result.style.display="none";setup.classList.remove("hidden");});window.addEventListener("pagehide",()=>{window.clearTimeout(timer);token++;});})();
</script></body></html>`;
