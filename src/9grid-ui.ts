import type { AuthUser } from "./auth";

const esc = (value: string): string => value.replace(/[&<>]/g, (char) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;" }[char] ?? char));
const userLabel = (user: AuthUser | null): string => user?.nickname ? esc(user.nickname) : user ? `USER #${user.id}` : "GUEST";

export const render9GridPage = (user: AuthUser | null): Response => {
  const html = `<!doctype html><html lang="ko"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>9Grid — 9 Grid Survival</title><style>
:root{color-scheme:dark;font-family:Inter,system-ui,sans-serif;background:#070a0d;color:#e8eef2;--surface:#0d1217;--line:#2b3943;--muted:#8d9ba4;--cyan:#63d7ff;--green:#65e6a5;--red:#ff7373}*{box-sizing:border-box}body{margin:0;min-height:100vh;background:radial-gradient(circle at 50% 0,#14212a,#070a0d 42%);padding:20px}.app{max-width:1240px;margin:auto}.top,.panel,.turnbar{border:1px solid var(--line);border-radius:10px;background:rgba(13,18,23,.97)}.top{display:flex;justify-content:space-between;gap:18px;padding:16px 18px;margin-bottom:14px}.brand{font-size:19px;font-weight:900;letter-spacing:2px}.subtitle,.user,.hint{color:var(--muted);font-size:11px}.subtitle{margin-top:5px}.user a{color:var(--cyan);text-decoration:none}.status{display:grid;grid-template-columns:repeat(9,minmax(80px,1fr));gap:8px;margin-bottom:12px}.stat{min-height:70px;padding:10px;border:1px solid var(--line);border-radius:8px;background:var(--surface)}.label{font-size:9px;font-weight:800;letter-spacing:1px;color:var(--muted)}.value{margin-top:5px;font-size:18px;font-weight:900}.detail{margin-top:4px;font-size:9px;color:var(--muted)}.monster-stat{border-color:#703b43}.monster-stat .value{color:var(--red)}.phase-stat{border-color:#315c69}.phase-stat .value{color:var(--cyan);font-size:12px}.turnbar{padding:12px 14px;margin-bottom:14px}.turnbar-head{display:flex;justify-content:space-between;margin-bottom:8px}.turnbar-title{font-size:10px;font-weight:900;letter-spacing:1px}.turnbar-caption{font-size:10px;color:var(--muted)}.steps{display:grid;grid-template-columns:repeat(4,1fr);gap:5px}.step{padding:7px;text-align:center;border:1px solid #30414a;border-radius:5px;color:#71828b;font-size:10px;font-weight:800}.step.active{border-color:var(--cyan);color:var(--cyan);background:#10232c}.step.done{border-color:#426b59;color:var(--green)}.layout{display:grid;grid-template-columns:1.08fr .92fr;gap:14px}.panel{padding:16px}.panel-head{display:flex;justify-content:space-between;align-items:center;margin-bottom:12px}.panel h2{margin:0;font-size:12px;letter-spacing:1.4px}.badge{padding:4px 8px;border:1px solid var(--line);border-radius:999px;font-size:9px;color:var(--muted)}.monster-info{display:flex;justify-content:space-between;align-items:center;margin-bottom:12px;padding:11px;border:1px solid #63383e;border-radius:8px}.monster-info small{display:block;color:#9f7474;font-size:9px}.monster-info .value{color:var(--red);font-size:23px}.board-wrap{display:flex;justify-content:center}.board{display:grid;grid-template-columns:repeat(3,minmax(80px,1fr));gap:8px;width:min(100%,500px)}.slot{position:relative;aspect-ratio:1;border:1px solid #3a4b56;border-radius:9px;background:#101820;color:#e8eef2;display:flex;flex-direction:column;align-items:center;justify-content:center;gap:4px;cursor:pointer}.slot:hover{border-color:var(--cyan)}.slot.empty{border-style:dashed;color:#52636d}.slot b{font-size:12px}.slot span{font-size:10px;color:var(--muted)}.slot .num{position:absolute;top:6px;left:7px;font-size:9px;color:#60727c}.synergy{border-top:1px solid var(--line);margin-top:12px;padding-top:11px}.synergy-group{margin-bottom:8px}.synergy-title{margin-bottom:4px;color:var(--muted);font-size:9px;font-weight:800;letter-spacing:1px}.synergy-line{display:inline-block;margin-right:5px;padding:5px 7px;border:1px solid #385467;border-radius:5px;background:#101d25;color:#a9ddf0;font-size:9px;font-weight:800}.cards{display:grid;grid-template-columns:repeat(3,1fr);gap:8px}.card{min-height:110px;padding:12px;text-align:left;border:1px solid #3a4b56;border-radius:8px;background:#101820;color:#e8eef2;cursor:pointer}.card:hover,.card.selected{border-color:var(--cyan)}.card.selected{box-shadow:0 0 0 1px var(--cyan)}.card-name{font-size:12px;font-weight:900}.card-job{margin-top:5px;color:var(--muted);font-size:10px}.card-number{float:right;color:#64757e;font-size:9px}.actions{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:12px}.actions button:last-child{grid-column:1/-1}button{font:inherit;font-weight:800;color:#e8eef2;background:#131c22;border:1px solid #3a4b56;border-radius:7px;padding:10px 12px;cursor:pointer}button:hover:not(:disabled){border-color:var(--cyan)}button.primary{border-color:#39708a;color:#bdeeff}button.combat{border-color:#75454b;color:#ffb1b1}button:disabled{opacity:.35;cursor:not-allowed}.log-panel{margin-top:14px}.log{height:170px;overflow:auto;border:1px solid var(--line);border-radius:7px;background:#080d11;padding:10px;font-family:ui-monospace,Consolas,monospace;font-size:11px;line-height:1.7}.log div{padding:1px 0}.danger{color:#ff8888}@media(max-width:1000px){.status{grid-template-columns:repeat(5,1fr)}}@media(max-width:760px){body{padding:8px}.top{flex-direction:column}.status{grid-template-columns:repeat(3,1fr)}.layout{grid-template-columns:1fr}.cards{grid-template-columns:1fr}.actions{grid-template-columns:1fr}.actions button:last-child{grid-column:auto}.steps{grid-template-columns:repeat(2,1fr)}}
</style></head><body><main class="app"><header class="top"><div><div class="brand">9GRID <span style="color:var(--cyan)">//</span> SURVIVAL</div><div class="subtitle">9턴 안에 몬스터를 처치하세요 · REROLL → SELECT → PLACE → COMBAT</div></div><div class="user">${userLabel(user)} · <a href="/">HOME</a></div></header><section class="status"><div class="stat"><div class="label">ROUND</div><div class="value" id="round">-</div></div><div class="stat"><div class="label">TURN</div><div class="value" id="turn">-</div></div><div class="stat"><div class="label">PLAYER HP</div><div class="value" id="hp">-</div></div><div class="stat"><div class="label">PLAYER ATK</div><div class="value" id="atk">-</div><div class="detail" id="atk-detail">WARRIOR LV0</div></div><div class="stat"><div class="label">DEF</div><div class="value" id="def">-</div><div class="detail" id="def-detail">TANK LV0</div></div><div class="stat"><div class="label">MANA</div><div class="value" id="mana">-</div><div class="detail" id="mana-detail">MAGE LV0</div></div><div class="stat"><div class="label">SCORE</div><div class="value" id="score">-</div></div><div class="stat monster-stat"><div class="label">MONSTER HP</div><div class="value" id="monster">-</div><div class="detail">ATK <span id="monster-attack-top">-</span></div></div><div class="stat phase-stat"><div class="label">CURRENT PHASE</div><div class="value" id="phase">NO SESSION</div></div></section><section class="turnbar"><div class="turnbar-head"><span class="turnbar-title">TURN FLOW</span><span class="turnbar-caption" id="turn-caption">GAME RESTART를 눌러 시작하세요</span></div><div class="steps"><div class="step" data-phase="reroll">1 · REROLL</div><div class="step" data-phase="select">2 · SELECT</div><div class="step" data-phase="placement">3 · PLACE</div><div class="step" data-phase="combat">4 · COMBAT</div></div></section><div class="layout"><section class="panel"><div class="panel-head"><h2>BOARD // 3 × 3</h2><span class="badge">PLACE SELECTED CARD</span></div><div class="monster-info"><div><div class="label">MONSTER ATTACK POWER</div><small>이번 전투에서 받는 기본 피해</small></div><div class="value" id="monster-attack">-</div></div><div class="board-wrap"><div class="board" id="board"></div></div><div class="synergy" id="synergy"></div></section><section class="panel"><div class="panel-head"><h2>CANDIDATES // 3 CARDS</h2><span class="badge" id="reroll-count">REROLL 0 / 1</span></div><div class="cards" id="cards"></div><div class="actions"><button id="reroll" class="primary">↻ REROLL ALL</button><button id="combat" class="combat">⚔ EXECUTE COMBAT</button><button id="start" class="primary">↻ GAME RESTART</button></div><div class="hint">REROLL은 모든 카드를 한 번에 교체합니다. 기본 1회이며 고블린 시너지 LV만큼 추가됩니다. 전투 후 다음 턴 카드는 자동으로 리롤됩니다.</div></section></div><section class="panel log-panel"><div class="panel-head"><h2>COMBAT LOG</h2><span class="badge">LIVE</span></div><div class="log" id="log"></div></section></main><script>
let state=null;let selectedCardId=null;let monsterAttack=null;const $=(id)=>document.getElementById(id);
function log(text,cls=""){const p=document.createElement("div");p.className=cls;p.textContent="> "+text;$("log").appendChild(p);$("log").scrollTop=$("log").scrollHeight}
async function request(path,options={}){const response=await fetch(path,{credentials:"same-origin",headers:{"Content-Type":"application/json",...(options.headers||{})},...options});let body=null;try{body=await response.json()}catch{}if(!response.ok)throw new Error(body?.error||"Request failed");if(Number.isFinite(body?.monsterAttack))monsterAttack=body.monsterAttack;return body}
function calculateSynergy(board){const races={};const jobs={};const add=(cards)=>{if(cards.length!==3||cards.some((card)=>!card))return;const race=cards.every((card)=>card.race===cards[0].race)?cards[0].race:null;const job=cards.every((card)=>card.job===cards[0].job)?cards[0].job:null;if(race)races[race]=Math.min(5,(races[race]||0)+1);if(job)jobs[job]=Math.min(5,(jobs[job]||0)+1)};for(let i=0;i<3;i+=1){add(board.slice(i*3,i*3+3));add([board[i],board[i+3],board[i+6]])}return {races,jobs}}
function updateFlow(phase){const order=["reroll","select","placement","combat"];document.querySelectorAll(".step").forEach((step)=>{const p=step.dataset.phase;step.classList.toggle("active",p===phase);step.classList.toggle("done",order.indexOf(p)>=0&&order.indexOf(p)<order.indexOf(phase))});const captions={reroll:"모든 후보 카드를 리롤하세요",select:"사용할 카드 1장을 선택하세요",placement:"선택한 카드를 보드에 배치하세요",combat:"전투를 실행하세요",game_over:"RUN 종료 · GAME RESTART로 1라운드부터 시작하세요"};$("turn-caption").textContent=captions[phase]||"게임을 시작하세요"}
function renderSynergy(s){const race=Object.entries(s.races).map(([name,level])=>'<span class="synergy-line">'+name.toUpperCase()+" LV"+level+"</span>").join("");const jobs=Object.entries(s.jobs).map(([name,level])=>'<span class="synergy-line">'+name.toUpperCase()+" LV"+level+"</span>").join("");$("synergy").innerHTML='<div class="synergy-group"><div class="synergy-title">RACE SYNERGY</div>'+(race||'<span class="hint">없음</span>')+'</div><div class="synergy-group"><div class="synergy-title">JOB SYNERGY</div>'+(jobs||'<span class="hint">없음</span>')+'</div>'}
function render(){if(!state){$("phase").textContent="NO SESSION";return}const r=state.round;const s=calculateSynergy(state.board);$("round").textContent=r.round;$("turn").textContent=r.turn+" / 9";$("hp").textContent=r.playerHp+" / "+r.playerMaxHp;$("atk").textContent=state.playerStats.attack;$("def").textContent=state.playerStats.defense;$("mana").textContent=state.playerStats.mana;$("atk-detail").textContent="WARRIOR LV"+(s.jobs.warrior||0);$("def-detail").textContent="TANK LV"+(s.jobs.tank||0);$("mana-detail").textContent="MAGE LV"+(s.jobs.mage||0);$("score").textContent=r.round*(s.races.dragon||0);$("monster").textContent=r.monsterHp+" / "+r.monsterMaxHp;$("phase").textContent=r.phase.toUpperCase();$("monster-attack").textContent=monsterAttack??"-";$("monster-attack-top").textContent=monsterAttack??"-";updateFlow(r.phase);renderSynergy(s);renderBoard();renderCards();const goblinLevel=s.races.goblin||0;$("reroll-count").textContent="REROLL "+r.candidates.rerollsUsed+" / "+(1+goblinLevel);$("reroll").disabled=(r.phase!=="reroll"&&r.phase!=="select")||r.candidates.rerollsUsed>=1+goblinLevel;$("combat").disabled=r.phase!=="combat";$("start").disabled=false}
function renderBoard(){const board=$("board");board.innerHTML="";state.board.forEach((card,index)=>{const button=document.createElement("button");button.className="slot"+(card?"":" empty");button.dataset.index=String(index);if(card){const num=document.createElement("span");num.className="num";num.textContent=String(index+1);const race=document.createElement("b");race.textContent=card.race.toUpperCase();const job=document.createElement("span");job.textContent=card.job.toUpperCase();button.append(num,race,job)}else{const num=document.createElement("span");num.className="num";num.textContent=String(index+1);const empty=document.createElement("b");empty.textContent="EMPTY";const hint=document.createElement("span");hint.textContent="PLACE HERE";button.append(num,empty,hint)}button.disabled=!selectedCardId||state.round.phase!=="placement";button.addEventListener("click",()=>place(index));board.appendChild(button)})}
function renderCards(){const cards=$("cards");cards.innerHTML="";state.round.candidates.cards.forEach((card,index)=>{const button=document.createElement("button");button.className="card"+(card.id===selectedCardId?" selected":"");const number=document.createElement("span");number.className="card-number";number.textContent="#"+(index+1);const name=document.createElement("div");name.className="card-name";name.textContent=card.race.toUpperCase();const job=document.createElement("div");job.className="card-job";job.textContent=card.job.toUpperCase();button.append(number,name,job);button.disabled=state.round.phase!=="select";button.addEventListener("click",()=>selectCard(card.id));cards.appendChild(button)})}
async function send(action,message){try{const body=await request("/api/games/9grid/session/action",{method:"POST",body:JSON.stringify(action)});state=body.state;selectedCardId=null;render();if(message)log(message)}catch(error){log(error instanceof Error?error.message:"Request failed","danger")}}
async function restart(){await send({type:"start"},"GAME RESTART · ROUND 1 / TURN 1")}
async function reroll(){await send({type:"reroll"},"REROLL ALL · 모든 후보 카드 교체")}
async function selectCard(cardId){try{const body=await request("/api/games/9grid/session/action",{method:"POST",body:JSON.stringify({type:"select",cardId})});state=body.state;selectedCardId=cardId;render();log("CARD SELECTED · 보드에 배치하세요")}catch(error){log(error instanceof Error?error.message:"Request failed","danger")}}
async function place(boardIndex){if(!selectedCardId)return;await send({type:"place",boardIndex},"CARD PLACED · COMBAT READY")}
async function combat(){await send({type:"combat"},"COMBAT COMPLETE · 다음 턴 카드 자동 준비")}
$("start").addEventListener("click",restart);$("reroll").addEventListener("click",reroll);$("combat").addEventListener("click",combat);
async function load(){try{const body=await request("/api/games/9grid/session");state=body.state;render();if(state)log("SESSION LOADED · ROUND "+state.round.round+" / TURN "+state.round.turn)}catch(error){log(error instanceof Error?error.message:"Session load failed","danger")}}
load();
</script></body></html>`;
  return new Response(html, { headers: { "Content-Type": "text/html; charset=utf-8", "Cache-Control": "no-store" } });
};
