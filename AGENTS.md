# kakao-account 개발 지침

## 프로젝트 목적
카카오 계정으로 사용자를 인증하고, 서비스 내부의 사용자 계정 및 사용자 데이터를 안전하게 관리하는 서버리스 웹 서비스 기반을 구축한다.

## 기본 원칙
- 모든 개발은 `main`의 최신 커밋을 기준으로 시작한다.
- 작업 시작 전 원격 `main`의 최신 SHA를 확인하고, 실제 반영 직전에도 최신 SHA를 다시 확인한다.
- 최신 `main`이 작업 기준과 달라졌다면 변경사항을 먼저 동기화한 뒤 작업한다.
- 기존 인증/OAuth/세션 동작을 확인하지 않고 핵심 흐름을 임의로 재작성하지 않는다.
- 비밀값(REST API Key, Client Secret, 세션 서명 키 등)을 저장소에 커밋하지 않는다.
- 카카오 Access Token을 장기 사용자 인증 정보처럼 DB에 저장하지 않는다. 서비스 세션과 외부 OAuth 인증을 분리한다.
- 카카오 고유 사용자 ID는 외부 식별자로만 사용하고, 서비스 내부에서는 자체 `users.id`를 기준 식별자로 사용한다.
- 문서와 구현이 충돌하면 구현보다 설계 의도를 먼저 검토하고 문서를 갱신한다.
- 최소 권한, 입력 검증, CSRF/세션 보호, 에러 정보 노출 방지를 기본 보안 요구사항으로 적용한다.

## TypeScript 구현 규칙
TypeScript 문법 오류와 CI 실패를 줄이기 위해 **코드 작성 → 포맷/문법 확인 → 타입 확인 → 테스트** 순서를 지킨다.

### 1. 문법 안전성
- 한 번에 지나치게 긴 한 줄의 TypeScript/HTML 템플릿을 작성하지 않는다. 복잡한 HTML은 작은 함수/상수로 분리한다.
- 중첩된 객체, 배열, 함수 호출, 삼항 연산자를 한 줄에 몰아넣지 않는다.
- 템플릿 문자열(`) 내부의 `${...}`와 일반 문자열의 따옴표를 명확히 구분한다.
- 문자열 안에서 불필요한 escape(`\\"`, `\\'` 등)를 사용하지 않는다. ESLint `no-useless-escape`를 통과해야 한다.
- 객체/배열/함수 호출을 작성한 뒤 괄호 `()`, 중괄호 `{}`, 대괄호 `[]`, 템플릿 문자열 `` ` ``의 짝을 즉시 확인한다.
- 복잡한 문자열 조립보다 배열 + `.join()` 또는 별도 렌더 함수 사용을 우선한다.
- 기존 파일을 크게 재구성할 때는 전체 파일을 교체하기 전에 현재 파일 구조와 import/export를 확인한다.

### 2. 타입 안전성
- `any`를 기본적으로 사용하지 않는다. 불가피한 경우 사용 이유와 범위를 최소화한다.
- 빈 `interface`를 만들지 않는다. 확장이 필요 없으면 `type` 또는 기존 타입을 직접 사용한다.
- 함수 매개변수와 반환값은 가능한 한 명시적으로 타입을 정의한다.
- `null`/`undefined` 가능성을 코드에서 명시적으로 처리한다.
- `as` 타입 단언으로 오류를 숨기지 말고 실제 타입을 개선한다.
- API 요청/응답 JSON은 신뢰하지 않는다. 런타임 검증 후 내부 타입으로 변환한다.
- Cloudflare `Env`, D1 결과 등 외부 경계의 타입은 실제 프로젝트 정의와 일치시킨다.

### 3. import/export 규칙
- 사용하지 않는 import와 변수를 남기지 않는다.
- 동일한 이름의 클래스/함수/타입을 import와 로컬 선언에서 중복 사용하지 않는다.
- 새 모듈을 만들면 export 형태와 실제 import 형태를 즉시 확인한다.
- 순환 의존성이 생기지 않도록 UI → 상태/도메인 → 저장소/인프라 방향을 우선한다.

### 4. 파일 수정 규칙
- 수정 전 대상 파일 전체 구조를 확인한다.
- 한 파일에서 여러 책임을 동시에 변경하지 않는다. 특히 인증, 세션, DB, UI를 한 번에 재작성하지 않는다.
- 새 기능은 가능한 한 작은 순수 함수부터 구현하고 테스트를 추가한 뒤 기존 라우트에 연결한다.
- 기존 동작을 보존하는 최소 변경을 우선한다.
- 대형 템플릿/HTML은 문자열 리터럴을 무리하게 한 줄로 압축하지 않는다.

## CI 검증 게이트
코드를 `main`에 반영하기 전에 반드시 다음 순서로 확인한다.
1. ESLint 문법/스타일 오류 확인
2. TypeScript type-check 확인
3. 단위 테스트 전체 실행
4. 필요 시 빌드/배포 설정 검증
5. 실패 시 실패 원인을 수정한 뒤 처음부터 다시 검증

특히 **Lint가 실패한 상태에서 TypeScript 오류가 없다고 판단하지 않는다.** Parse Error는 최우선으로 수정한다.

## 개발 단계
1. 설계 문서 확인 및 변경 범위 결정
2. Cloudflare Worker/D1 기본 프로젝트 생성
3. Kakao Developers 앱 및 Redirect URI 설정
4. OAuth 인가 코드 → 토큰 → 사용자 정보 흐름 구현
5. 사용자 생성/조회 및 서비스 세션 구현
6. `/api/me`, 로그아웃 등 인증 API 구현
7. DB 마이그레이션 및 테스트
8. GitHub Actions 배포 자동화
9. 9Grid 게임 및 관련 API 구현
10. 운영 보안/관측성/비용 한도 점검

## 테스트 원칙
- 인증 성공/취소/실패/재로그인/로그아웃을 검증한다.
- 동일한 카카오 사용자에 대해 중복 계정이 생성되지 않아야 한다.
- 잘못된 OAuth state 및 만료/유효하지 않은 세션을 거부한다.
- 로컬/Preview/Production 환경의 비밀값과 Redirect URI를 분리한다.
- 순수 로직은 가능한 한 단위 테스트로 검증한다.
- 새 API는 정상 입력뿐 아니라 빈 값, 잘못된 타입, 범위 초과, 인증되지 않은 요청을 테스트한다.
- UI 변경 후에는 최소한 TypeScript 문법, 타입, 관련 테스트를 다시 실행한다.

## GitHub 반영 절차
- 작업 시작: 원격 `main` 최신 SHA 확인
- 구현: 관련 파일만 최소 수정
- 검증: lint → type-check → test
- 실제 반영 직전: 원격 `main` 최신 SHA 재확인
- SHA가 변경되었다면 최신 `main` 기준으로 다시 동기화하고 검증한다.
- 검증을 통과한 변경만 `main`에 반영한다.
- 반영 후 GitHub Actions 결과를 확인하고 실패하면 원인을 추적하여 수정한다.
- 커밋 메시지는 변경 목적이 명확하게 드러나도록 작성한다.
